export const Background =(url,style)=>({

    backgroundImage: `url(${url})`,
    backgroundPositon: 'center',
    bacgroundSize: 'cover',
    backgroundRepeat: 'no-repeat',
    ...style

})

// NavBar
